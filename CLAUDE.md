## 하네스: HuggingFaceCrawler

**목표:** Hugging Face Hub에서 `data/companies.json`에 등록된 회사(주로 한국 AI 기업, 목록은 계속 늘어남 — 48개로 고정된 숫자가 아님)가 업로드한 모델을 주 1회 자동 크롤링하고, 이전 대비 신규/업데이트분만 정제해 엑셀로 만들어 이메일 발송한다. LLM 토큰은 최소 오케스트레이션 + 2차(AI) 정제 단계에서만 사용하고, 나머지는 결정론적 파이썬 스크립트로 처리한다.

**트리거:** 크롤링/정제/엑셀/메일 발송 등 이 프로젝트의 파이프라인 관련 요청이면 `hf-pipeline` 스킬을 사용하라 (전체 실행, 재실행, 부분 재실행 모두 포함). 특정 단계 하나만 디버깅하려면 `hf-crawl`/`hf-refine`/`hf-excel`/`hf-email` 스킬을 개별적으로 사용한다. 단순 질문(예: "지금 회사 몇 개 등록돼있어?")은 직접 응답 가능.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-07-19 | 초기 하네스 구성 (crawl/refine/excel/email 4개 에이전트+스킬, hf-pipeline 오케스트레이터) | 전체 | 토큰 최소화를 위해 결정론적 스크립트+단일 에이전트 순차 실행 구조로 설계. 실제 scripts/*.py는 아직 미구현 — 각 스킬의 "스크립트 계약" 절대로 다음 단계에서 작성 예정. 마스터 데이터셋 스키마와 2차 AI 정제 내용은 실제 크롤링 데이터를 본 뒤 확정하기로 보류. |
| 2026-07-19 | `scripts/crawl.py` 구현 완료, 마스터/엑셀 26개 필드 스키마 확정(1차/2차 담당, 통제 어휘, 벤치마크 카테고리 분리, 제외 정책) | scripts/crawl.py, .claude/skills/hf-refine, .claude/skills/hf-excel, .claude/agents/* | 실제 크롤링 샘플(tunib/naver-hyperclovax/upstage)을 사용자와 함께 검토해 필드·형식·필수값을 확정. 규모/모델설명/라이선스 중 하나라도 없으면 엑셀에서 제외하기로 함. |
| 2026-07-19 | 로그 체계를 crawl_log.jsonl(기술로그, 매회 기록)과 pipeline_history.jsonl(공식 이력, 전체 파이프라인 완주시에만 기록)로 분리 | scripts/crawl.py 호출부, .claude/skills/hf-pipeline, .claude/skills/hf-email | 샘플 확인용 수동 크롤링이 "공식 실행 이력"에 섞여 들어가는 것을 방지하기 위해. 기존 logs/run_log.jsonl(탐색 단계 테스트 기록)은 삭제함. |
| 2026-07-19 | refine/merge/build_excel/send_email 4개 스크립트 구현 완료, tunib+naver-hyperclovax 9개 모델로 실제 end-to-end 검증(제외 판정·이메일 발송 포함) | scripts/*.py | 실제 라이선스 누락 모델(tunib)이 정확히 제외되는 것과 엑셀 null→빈칸 렌더링을 실측으로 확인 |
| 2026-07-19 | 특화 도메인을 산업(적용분야) 축 13종으로 확정, 지원 모달리티는 2차가 항상 덮어쓸 수 있게 변경, 지원 언어에 "인코더 제외 일반 LLM은 영어 기본 포함" 규칙 추가, 벤치마크가 이미지로만 있는 경우 최종 포함 확정된 모델에 한해 이미지를 직접 열어보는 3차 단계 추가 | .claude/skills/hf-refine, scripts/refine.py(benchmark_image_urls 탐지 정규식) | 실 데이터에서 커스텀 VLM의 pipeline_tag 오판, 도메인·모달리티 축 혼재, 일부 모델의 벤치마크가 이미지(막대그래프/레이더차트)로만 제공되는 문제를 실제로 발견해 수정 |
| 2026-07-19 | 엑셀 파일명에 날짜 자동 삽입, "모델 ID"(원본 키)/"모델명"(표시용) 컬럼 분리, 이미지 감지 범위를 Benchmark뿐 아니라 Evaluation/Eval Results까지 확대, 벤치마크 카테고리를 11종(종합 제거, 진실성→안전성·신뢰성, 비전·음성·에이전틱 신설)으로 재설계하고 한국어는 원 주제 카테고리에 추가로 한 번 더 기재하는 방식으로 변경 | scripts/refine.py, scripts/build_excel.py, .claude/skills/hf-refine, .claude/skills/hf-excel | 모델 ID가 향후 재크롤링 매칭 키가 되어야 함, 벤치마크 필터링/검색이 목적이라 "종합"처럼 비교 불가능한 값은 제거하고 한국어 벤치마크가 주제 검색에서 숨는 문제를 이중 기재로 해결 |
| 2026-07-19 | 엑셀 null 표시를 빈칸→"미기재" 문자열로 재변경(정렬은 null 상태로 먼저 수행 후 채움), 벤치마크 11개 카테고리를 4개 주제(언어·지식이해/전문·논리능력/멀티모달이해/안전성·신뢰성)+한국어(additive)+기타로 통합, 지원 모달리티를 한글 고정조합 대신 영어 기본값 5개(Text/Image/Video/3D/Audio) 콤마 나열 방식으로 재설계(Vision이 이미지/비디오/3D를 뭉뚱그리던 문제 해결), 테스트 중 쌓인 logs/*.json·master_dataset.jsonl·output/*.xlsx·state/·raw/ 전부 초기화 | scripts/refine.py, scripts/build_excel.py, .claude/skills/hf-refine, .claude/skills/hf-excel | 벤치마크 카테고리가 검색 목적에 비해 너무 잘게 쪼개져 있었음, 모달리티도 나중에 필터로 쓸 것이므로 영어로 통일, 실제 51개 회사 크롤링 전 깨끗한 상태에서 시작하기 위해 |
| 2026-07-19 | 모델 아키텍처 필드에서 `config.model_type` 접두어 제거(architectures[0] 단독 사용), "기술 리포트 링크" 필드 신규 추가(arxiv 태그/PDF 파일/README 링크 우선순위로 감지, 클릭 가능한 링크로 정규화) | scripts/refine.py, scripts/build_excel.py, .claude/skills/hf-refine, .claude/skills/hf-excel | naver-hyperclovax 실 데이터에서 동일 아키텍처(HCXVisionV2ForCausalLM)인데 model_type이 릴리스마다 달라 다른 아키텍처처럼 보이는 문제 발견. 기술 리포트 존재 여부가 3개 독립 신호(arxiv 태그/PDF sibling/README 링크)로 안정적으로 판별됨을 실측 확인 |
| 2026-07-19 | 이메일 본문을 HTML 표로 변경(회사/모델/상태(신규 등록·업데이트·정보부족으로 미등재)/비고, 제외가 신규·업데이트보다 우선), 벤치마크에 "카테고리 평균만 있고 개별 벤치마크명이 없으면 null" 규칙 추가(Think-32B 사례 반영), 벤치마크 점수를 0~100 스케일로 통일(0~1 소수는 ×100, MT-Bench/LogicKor 같은 고유 척도는 그대로 유지) | scripts/send_email.py, .claude/skills/hf-email, .claude/skills/hf-refine | 어떤 회사/모델을 확인했는지 엑셀을 열지 않고도 메일 본문에서 바로 볼 수 있어야 함, naver-hyperclovax는 0~1 소수로 보고하는데 upstage/tunib은 0~100이라 스케일이 안 맞았음, 뭉뚱그린 카테고리 평균은 "종합"과 마찬가지로 비교 불가능한 값이라 제외 |
| 2026-07-19 | "모델 성격"을 "모델 구축 방식"으로 개명하고 통제 어휘를 9종(베이스/파인튜닝/경량화·증류/병합/양자화/어댑터/포맷변환·포팅/토크나이저·보조컴포넌트/기타)으로 확장, "판단불가"는 "기타"가 아니라 null로 구분. `hf-report` 에이전트+스킬 신규 추가(기술 리포트 PDF를 섹션별로 분해해 최종 포함 확정+정보공백 있는 모델만 필요한 섹션 하나만 읽어 보강), `scripts/extract_report_sections.py` 구현(pdfplumber 기반, arxiv abs→pdf 자동변환) | .claude/agents/hf-report.md, .claude/skills/hf-report/SKILL.md, scripts/extract_report_sections.py, scripts/refine.py(model_nature→construction_method), .claude/skills/hf-pipeline, requirements.txt | naver-hyperclovax 실 데이터에서 "기타"가 실은 "경량화·증류"(Think-14B) 분류 공백과 "판단불가"(Omni-8B)가 섞여있던 문제 발견. Omni-8B 기술 리포트를 실제로 다운로드해 검증한 결과 Evaluation 섹션에서 README 이미지로는 못 얻은 실제 벤치마크 수치를 확보했고, Post-Training 섹션에서 "pre-trained backbone을 SFT"라는 명시적 문구로 구축방식을 베이스→파인튜닝으로 정정 |
| 2026-07-19 | 3개 회사(tunib+naver-hyperclovax+Supertone) 12개 모델로 crawl→refine→AI 2차→merge→excel→email 전체 end-to-end 재검증(수동 테스트라 pipeline_history.jsonl 미기록), 이메일 본문을 모델별 표→회사별 한 줄 요약(총 N건 중 신규/업데이트/미등재)으로 축소, modality 필드는 1차가 채웠어도 항상 2차 재확인 대상에 강제 포함(VLM/옴니모델 오탐 방지), 벤치마크 필드에서 추론속도·처리량 지표(RTF 등) 배제 명시 | scripts/refine.py(needs에 modality 강제 추가), scripts/send_email.py(build_company_summaries), .claude/skills/hf-refine, .claude/skills/hf-email | 실제로 Omni-8B/Think-32B/Vision-Instruct-3B 3개 모두 pipeline_tag="text-generation"만 보고 modality가 Text로 오분류돼있던 것과 Supertone TTS 모델의 속도 비교표가 벤치마크_기타에 잘못 들어간 것을 실측으로 발견 |
| 2026-07-19 | 엑셀 null 표시를 "미기재" 문자열→빈 칸으로 재변경(재반전), 공개일/최근업데이트일은 실제 datetime 타입, 규모/컨텍스트길이는 실제 numeric 타입(정수 콤마 포맷)으로 기록되도록 수정. 로컬 자격증명은 `.env`(gitignore 대상)로 관리하고 `crawl.py`/`send_email.py`가 `python-dotenv`로 자동 로드하도록 변경(예약 실행 시 사람이 매번 환경변수를 넣어줄 수 없으므로). "기록 초기화" 요청 시 raw/state/master_dataset/output/logs를 전부 지우고 git에도 반영하는 절차를 hf-pipeline에 명시. raw/state/master_dataset.jsonl/output 전부 삭제해 한 번도 크롤링하지 않은 상태로 복귀(51개 회사 본 크롤링은 GitHub 프로젝트 연동 예약 루틴으로 실행할 예정이라 로컬 테스트 산출물을 남기지 않기로 함) | scripts/build_excel.py, scripts/crawl.py, scripts/send_email.py, requirements.txt(python-dotenv), .env, .claude/skills/hf-excel, .claude/skills/hf-refine, .claude/skills/hf-pipeline | 사용자가 "미기재"보다 빈 칸이 낫다고 재차 판단, 날짜/숫자가 텍스트로 기록돼 엑셀에서 정렬·필터가 안 되는 문제 발견. 예약 실행이 로컬 파일이 아니라 GitHub 프로젝트 연동으로 도는 방식임이 확인되어, 로컬 `.env`만으로는 그 환경에 자격증명이 전달되지 않는다는 점도 함께 확인(후속 조치 필요) |
| 2026-07-19 | GitHub 저장소 Actions secrets(repo settings)에 등록한 것만으로는 예약 루틴 실행 환경에 자격증명이 전달되지 않음을 실제 실행으로 확인 — 루틴이 1단계(crawl)에서 `HF_TOKEN not set`으로 정상적으로 멈추고, 토큰 조작·가짜 커밋 없이 깨끗하게 실패 보고한 것도 확인(설계된 에러 정책이 의도대로 동작). `pip install -r requirements.txt`를 Phase 0 맨 앞 단계로 명시 추가 | .claude/skills/hf-pipeline | 실제 예약 루틴 1회 실행 결과, 이 루틴은 GitHub Actions secrets가 아니라 별도의 "Claude Code on the web" 프로젝트 환경 설정에서 시크릿을 읽는 것으로 보임(루틴 자체가 이렇게 보고함) — 다음 확인 단계는 그 환경 설정에 세 값을 등록하는 것. 또한 실행 환경이 매번 새 컨테이너라 의존성이 남아있지 않다는 것도 실측으로 확인(첫 실행에서 dotenv 등 누락) |
| 2026-07-19 | 이 저장소에는 `.github/workflows/`가 아예 없음을 확인 — 즉 "Claude Code 루틴"은 GitHub Actions가 아니라 Anthropic이 관리하는 별도 예약 실행 시스템이 저장소를 clone/pull만 해오는 구조. 환경변수를 Claude Code on the web 프로젝트 설정(Actions secrets 아님)에 등록한 뒤 재실행 → 자격증명/의존성 문제는 해결됐으나 새로운 두 차단 확인: ① egress 네트워크 정책이 `huggingface.co`를 allow-list에 안 넣어 크롤링 자체가 403으로 막힘(SMTP도 같은 문제 예상), ② 이 루틴이 쓰는 GitHub 연동이 읽기 전용이라 git push/GitHub 앱 둘 다 403 — 실패해도 가짜 커밋·이력 기록 없이 깨끗하게 멈추는 것은 이번에도 확인됨 | (문서화만, 코드 변경 없음) | 사용자 환경 설정 문제이며 코드로 해결 불가 — 다음 확인 사항: (a) Claude Code on the web 프로젝트의 네트워크/egress 정책에 `huggingface.co`/`cdn-lfs.huggingface.co`/`smtp.gmail.com` allow-list 추가, (b) 이 루틴이 쓰는 GitHub 연동(앱)의 저장소 권한을 Read-only → Read/write로 변경 |
| 2026-07-19 | HF Hub egress는 열렸음을 확인(200 OK), 루틴이 첫 전체 크롤링(51개 회사, 632개 신규 모델)을 완료. 단 GitHub 연동이 여전히 read-only라 master에 못 올리고 새 브랜치(`claude/dazzling-goodall-lnt3ro`)에 체크포인트 커밋만 남긴 채 정지, 2차 AI 정제 대상 563건은 한 세션에서 못 끝낸다고 보고. 로컬 대화형 세션(이 세션)은 그 루틴과 별개의 자격증명(`gh auth`)을 쓰므로 master 직접 push가 되는 것을 확인 → 브랜치를 master로 fast-forward 병합·push함. 근본 원인도 함께 발견: `pending_ai_inputs.json`이 매 실행마다 그 주 diff만으로 통째로 덮어써지고 `merge_ai_summaries.py`가 처리분을 지우지 않아서, 대기열이 한 세션에 못 끝나면 다음 실행 때 조용히 유실되는 구조였음(2차 AI 정제가 "전부 끝내야만 저장되는" 원자적 단일 스텝이었던 것) | scripts/refine.py(대기열 이월), scripts/merge_ai_summaries.py(--pending-file로 처리분 제거·체크포인트), .claude/skills/hf-refine(§배치 처리·체크포인트, 25~40개 배치+매 배치 후 merge), .claude/skills/hf-pipeline(3~4번 단계 배치 반복, 에러 핸들링표 갱신) | 최초 전체 크롤링처럼 대기열이 설계 규모(주간 소량 증분)를 크게 벗어나는 상황이 실제로 발생했고, 앞으로도 회사가 한 번에 여러 모델을 올리는 경우 재발 가능 — "한 세션 안에 다 못 끝내면 실패"가 아니라 "몇 번에 걸쳐서라도 반드시 끝나는" 구조로 바꿔 이 문제 자체를 근본적으로 없앰. 실제 632건/563건 백로그로 배치·이월 로직을 시뮬레이션 검증함(스크래치패드, repo 오염 없음) |
| 2026-07-19 | 배치 체크포인트만으로는 "오케스트레이터 한 에이전트의 컨텍스트가 대규모 백로그를 못 버틴다"는 근본 스케일 한계가 안 풀린다는 점을 사용자가 지적(순차 배치는 몇 주에 걸쳐 조금씩만 줄어듦, 매번 사람이 수동으로 판단해줘야 하는 구조였음) — 대기열 100건을 기준으로 오케스트레이터가 자동으로 순차/병렬 모드를 전환하도록 변경. 병렬 모드는 대기열을 30개 청크로 나눠 웨이브당 최대 6개 서브에이전트를 `Agent` 도구로 동시 스폰하되, 서브에이전트는 JSON만 반환하고 파일 쓰기는 절대 하지 않으며 `master_dataset.jsonl`/`pending_ai_inputs.json` 반영은 오케스트레이터 혼자 순차적으로만 수행(경쟁 조건 방지) | .claude/skills/hf-refine(§대규모 백로그: 자동 병렬 처리), .claude/skills/hf-pipeline(실행 모드 절, 3번 단계 100건 분기) | 이례적으로 큰 백로그가 첫 크롤링 외에도(회사 대량 추가, 한 회사의 대량 업로드 등) 재발할 수 있는데, 그때마다 채팅으로 "병렬 처리해도 될까요?" 물어보게 하지 않고 대기열 크기만으로 자동 결정되게 하려는 것. 이 세션에서 사용자가 GitHub App 권한(Read/write)과 클라우드 프로젝트 네트워크 액세스("전체")를 직접 조정한 스크린샷 확인 — 다음 예약 실행부터 egress/push 차단이 실제로 풀리는지는 아직 미확인(환경 변경은 새 세션부터 적용됨) |
| 2026-07-19 | 사용자가 "그 네트워크/GitHub App 설정은 지난번 403이 나기 전부터 이미 그렇게 돼 있었다"고 확인 — 즉 §네트워크/GitHub 연동 문제가 단순 설정 누락이 아니라 다른 원인일 가능성. `raw/state/output/logs/master_dataset.jsonl` 전부 삭제해 한 번도 크롤링 안 한 상태로 복귀(§기록 초기화 절차) — 다음 예약 실행 결과를 이 세션의 수동 체크포인트/백로그 작업과 섞이지 않은 깨끗한 검증 기준으로 쓰기 위함 | (초기화만, 코드 변경 없음) | 다음 예약 실행이 처음부터 끝까지(크롤→정제→엑셀→메일→push) 성공하는지가 곧 네트워크/GitHub 설정이 실제로 유효한지의 유일한 검증 수단 |
| 2026-07-19 | Claude Code 공식 문서 + `anthropics/claude-code#50146`(오픈 이슈) 조사 결과: huggingface.co는 예약 실행에서 실제로 열려있음이 확인됐지만(632개 모델 크롤링 전례), smtp.gmail.com은 예약 세션이 프로젝트의 "네트워크 액세스" 설정과 무관한 별도의 고정 allowlist를 쓰기 때문에 막혀있는 것으로 보이고 사용자가 직접 고칠 설정이 없음(#50146이 이 기능 자체를 요청하는 진행 중인 이슈). git push도 App 권한과 무관하게 "현재 작업 브랜치"로만 허용되는 것으로 보이며, 지난 회차에 루틴이 master 대신 새 브랜치에 커밋한 것도 이 제약과 일치 — 고장이 아니라 설계된 동작. 사용자가 "엑셀 생성이 최우선, 이메일/push가 막혀도 작업을 중도에 끊지 말라"고 지시 → hf-pipeline에 "엑셀 생성(7번)까지는 무조건 완주", "3번 대기열이 안 비어도 5~7번은 진행", "push 거부 시 가능한 브랜치로 대체 push, 그마저 안 되면 로컬 커밋 유지+눈에 띄게 보고" 원칙을 명문화 | .claude/skills/hf-pipeline(핵심 원칙 절, §네트워크/GitHub 제약 절, 9번 단계, 에러 핸들링표, 미확정 부분에 10번 조건 관련 메모 추가) | SMTP 차단과 master 직접 push 제한이 이 프로젝트 설정으로 못 고치는 Claude Code 제품 한계로 확인된 이상, 파이프라인이 그걸 "실패"로 취급해 엑셀 산출물까지 못 만드는 일이 없도록 하는 게 핵심. `pipeline_history.jsonl` 기록 조건(이메일 성공 필수)이 이 제약 때문에 영원히 안 채워질 수 있다는 점은 사용자 결정 없이 임의로 바꾸지 않고 미확정 항목으로만 남겨둠 |
| 2026-07-20 | 로컬 master를 원격(예약 루틴이 완주한 첫 전체 크롤링 결과, PR #4)과 fast-forward 동기화. `data/companies.json`의 한글 회사명 3건 정정(시오닉에이아이→사이오닉AI, 알간이즈→올거나이즈, 데이터모→셀렉트스타) 및 기존 `master_dataset.jsonl` 26개 모델 행에 소급 반영. "모델 구축 방식" 통제 어휘에서 괄호 설명을 저장값에서 제거(예: "파인튜닝 모델(SFT·RLHF·DPO 등 추가학습)" → "파인튜닝 모델")하고 기존 477개 모델 행에 소급 반영, 중복 표기(포맷 변환·포팅/토크나이저·보조 컴포넌트)도 통합. 두 변경 모두 엑셀 재생성 후 커밋·push 완료 | data/companies.json, data/master_dataset.jsonl, output/huggingface_models_2026-07-20.xlsx, .claude/skills/hf-refine | 사용자가 회사 한글명은 실제 널리 쓰이는 표기와 대조 검수를 요청, 구축 방식 필드는 통제 어휘 값 자체에 괄호 설명이 섞여 들어가 있던 것을 정리해달라고 요청. 두 값 모두 향후 정제 로직(AI 2차)이 그대로 재생산하지 않도록 스킬 문서까지 함께 수정 |
| 2026-07-20 | `logs/pipeline_history.jsonl` 기록 조건에서 "예약 스케줄에 의한 자동 실행이어야 한다"는 조건을 폐지 — 이메일 발송 성공 여부만으로 기록(트리거가 채팅 직접 요청이든 예약 스케줄이든 구분 안 함). 모델설명 작성 지침에 "적합" 어미 반복 금지 + 같은 계열(베이스 모델 동일, 크기/양자화/포맷만 다른) 모델은 설명을 완전히 동일하게 복사하지 않고 이미 아는 값(크기·양자화 방식 등)을 문장에 엮어 구분하는 규칙 추가, 기존 마스터 데이터셋에서 발견된 중복 설명 33그룹(모델 102건 — nota-ai Vicuna MLC 변환분, FriendliAI FP8/INT8 양자화분, kaist-ai LangBridge 계열 등)에 전부 소급 적용해 중복 0건으로 정리, 엑셀 재생성 | .claude/skills/hf-pipeline, .claude/agents/hf-pipeline, .claude/skills/hf-refine, data/master_dataset.jsonl, output/huggingface_models_2026-07-20.xlsx | 사용자가 예약 실행에서만 공식 이력을 남기는 규칙을 취소하고 이 세션(채팅) 실행도 기록하라고 요청. 또한 실제 데이터에서 같은 베이스 모델의 크기/양자화 변형들이 설명 문장을 통째로 복사한 것을 확인, 근거 없는 차별화 문구 대신 이미 알고 있는 크기/양자화/버전 정보로 자연스럽게 구분하도록 지침을 명확히 함 |
